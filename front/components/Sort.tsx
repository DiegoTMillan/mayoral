// export function handleSortAsc() {
//     let cardData = []
//   const sortedData = [...cardData].sort((a, b) => {
//     return a.value > b.value ? 1 : -1;
//   });
//   setCardData(sortedData);
// }

// export function handleSortDesc() {
//     let cardData = []
//   const sortedData = [...cardData].sort((a, b) => {
//     return a.value < b.value ? 1 : -1;
//   });
//   setCardData(sortedData);
// }
